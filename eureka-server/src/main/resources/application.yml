server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    # Dev-only: sweep for expired leases every 5s instead of the ~60s
    # default, so scaled-down/dead instances disappear faster in local demos.
    eviction-interval-timer-in-ms: 5000